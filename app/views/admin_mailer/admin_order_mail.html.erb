<h3>Salut! Une nouvelle Ordre a été commandée !</h3>
<p >L'utilisateur:</p>

id: <%= @user.id %></br>
Nom: <%= @user.first_name %> <%= @user.last_name %></br>
Email: <%= @user.email %></br>

Adresse: <%= @user.address %></br>


<p><%= @user.first_name %> a l'Ordre :</p>
<p>n° : <%= @order.id %> créé le <%= @order.created_at.strftime("%d-%m-%Y") %></br>

<div class="container-fluid pt-5">
      <div class="row px-xl-5">
          <div class="col-lg-12 table-responsive mb-5">
              <table class="table table-bordered text-center mb-0">

                  <thead class="border-white text-white">
                      <tr>
                          <th>Produit</th>
                          <th>Prix</th>
                          <th>Quantité</th>
                          <th>Total</th>
                      </tr>
                  </thead>

                  <% @cart.product_carts.sort.each do |product_cart| %>
                      <tbody class="align-middle">
                      <tr>
                          <td class="align-middle"><img src= "" alt="" style="width: 50px;"><%= product_cart.product.title %></td>
                          <td class="align-middle"><%= product_cart.product.price %> €</td>
                          <td class="align-middle"><%=product_cart.quantity%></td>
                          <td class="align-middle"><%= product_cart.total_price %> €</td>
                      </tr>
                      </tbody>
                  <% end %>      
              </table>
          </div>
      </div>
  </div>

  Paiement: <%= @order.cart.sub_total %></br>





